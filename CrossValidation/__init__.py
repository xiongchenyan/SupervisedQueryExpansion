'''
cross validation to 
    generate candidate terms, labels and extract features
        if already generated then load from disk
    train-test
        select parameter from train folds, and then classify test folds
        re-rank test folds and evaluate
'''